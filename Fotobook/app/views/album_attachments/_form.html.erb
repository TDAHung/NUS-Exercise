<%= form_with(model: album_attachment) do |form| %>
  <% if album_attachment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(album_attachment.errors.count, "error") %> prohibited this album_attachment from being saved:</h2>

      <ul>
        <% album_attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :album_id, style: "display: block" %>
    <%= form.number_field :album_id %>
  </div>

  <div>
    <%= form.label :img_url, style: "display: block" %>
    <%= form.text_field :img_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
